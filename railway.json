{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c 'php -S 0.0.0.0:\${PORT} -t public router.php'",
    "healthcheckPath": "/health.php",
    "healthcheckTimeout": 120,
    "sleepApplication": false
  }
}
